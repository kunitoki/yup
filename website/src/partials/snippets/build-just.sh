# https://github.com/casey/just wraps the common CMake workflows
just mac         # generate and open the Xcode project
just ninja       # Ninja multi-config
just emscripten  # build for Wasm
just test        # run the unit tests
